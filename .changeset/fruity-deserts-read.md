---
---

Add recorded screenshots to the CLI readme for keyboard navigation, the MCP loop, `--json`, and `--local`, plus the vhs tapes that generate them. Documentation and tooling only, nothing about the published CLI changes.
